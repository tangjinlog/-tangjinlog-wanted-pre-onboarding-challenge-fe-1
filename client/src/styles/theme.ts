import { Theme } from '@emotion/react';

const theme: Theme = {
  fontSizes: {
    sm: '14px',
    md: '16px',
    lg: '24px',
  },
  colors: {
    primary: 'gray',
    secondary: '#fff',
  },
};

export default theme;

import { Login } from 'components/Login';
import { Background } from 'components/Background';

const App = () => {
  return (
    <Background>
      <Login />
    </Background>
  );
};

export default App;
